export let gameConfig: {
  designWidth: number;
  designHeight: number;
  canvasWidth: number;
  canvasHeight: number;
  designRatio: number;
  deviceRatio: number;
  scaleMultiplier: number;
} = {
  designWidth: 540,
  designHeight: 960,
  canvasWidth: 540,
  canvasHeight: 960,
  designRatio: 1,
  deviceRatio: 1,
  scaleMultiplier: 1,
};
